{
  "name": "@fabianopinto/utils",
  "version": "0.1.0",
  "private": false,
  "license": "ISC",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup --clean",
    "dev": "tsup --watch",
    "clean": "rimraf dist || rm -rf dist",
    "lint": "echo 'no lint configured'",
    "test": "echo 'no tests yet'"
  },
  "sideEffects": false,
  "devDependencies": {
    "typescript": "^5.4.0",
    "rimraf": "^5.0.0",
    "@types/node": "^20.12.7",
    "tsup": "^8.1.0"
  }
}
